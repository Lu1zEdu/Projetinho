public class ChatManager {
    public void sendMessage(String message, String userName) {
        // Aqui você pode adicionar a lógica para enviar a mensagem para outros clientes
        // ou processar a mensagem de alguma forma.
        // Por enquanto, apenas imprime no console
        System.out.println("Mensagem enviada: " + message +" || por: " + userName);
    }
}
